"""检索链路的六个步骤，一个文件一步。

    改写 → 路由 → 过滤 → 召回融合 → 重排 → 装配

拆成六个模块不是为了好看，是为了**每一步都能单独观测和评测**。
「召回 top-k 然后拼 prompt」这句话掩盖了至少六次数据结构变换，而线上出坏
case 时，排查顺序恰恰是沿着这条链路倒着走一遍（README 第十章）：

| 症状 | 看哪一步 |
|---|---|
| 检索到的完全是另一类问题的条款 | 1 改写（意图拆错） |
| 该引平台条款却引了法条 | 2 路由（层级权重） |
| 明明有条款却一条没召回 | 3 过滤（生效日期把它排除了） |
| 召回了但排名靠后 | 4 召回融合（单路命中在 RRF 里天然吃亏） |
| 候选里有正确答案但没顶上来 | 5 重排 |
| 上下文里有证据但答复没用上 | 6 装配（被预算截断 / 排在中部） |
"""
