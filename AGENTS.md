Implement an evaluation-driven evolution loop agent.

## rules
- Keep the code simple. Implement only the required functionality and introduce abstractions only when necessary.
- Documentation should not sound AI-generated or contain filler.
- No `Co-Authored-By` in commit messages.

## markdown document
- Use `https://tiltwind.github.io/refund-agent` as the base URL for documentation links. 
- Use `https://github.com/tiltwind/refund-agent` as the base URL for source code links.
